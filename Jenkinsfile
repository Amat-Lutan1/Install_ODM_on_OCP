pipeline {
    agent any

    stages {
        stage('Initialization') {
            steps {
                echo 'Initialization'
                sh 'oc project odm-sandbox'
            }
        }
    }
}
